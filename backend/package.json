{
  "name": "comtec-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Backend ComTec - login, dashboard, ventas, inventario, chat",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node --no-warnings db/seed.js && node --no-warnings server.js",
    "dev": "node --no-warnings --watch server.js",
    "seed": "node --no-warnings db/seed.js"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "jsonwebtoken": "^9.0.2",
    "socket.io": "^4.7.5"
  }
}
